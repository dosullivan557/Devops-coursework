pipeline:
  name: IAC
  identifier: IAC
  projectIdentifier: default_project
  orgIdentifier: default
  description: Validate, review, and deploy the Azure Container Registry Bicep configuration
  tags: {}
  variables:
    - name: source_branch
      type: String
      description: Git branch containing the Bicep configuration to deploy
      required: true
      value: <+input>.default(main)
    - name: azure_location
      type: String
      description: Azure deployment metadata location
      required: true
      value: uksouth
  stages:
    - stage:
        name: Plan and deploy
        identifier: Plan_and_deploy
        type: Custom
        description: Preview and approve Azure infrastructure changes before deployment
        spec:
          execution:
            steps:
              - step:
                  type: ShellScript
                  name: Bicep validate and what-if
                  identifier: Bicep_validate_and_what_if
                  spec:
                    shell: Bash
                    executionTarget: {}
                    delegateSelectors:
                      - local-delegate
                    source:
                      type: Inline
                      spec:
                        script: |-
                          set -eu

                          REPOSITORY_URL="https://github.com/dosullivan557/Devops-coursework.git"
                          SOURCE_BRANCH='<+pipeline.variables.source_branch>'
                          AZURE_CLI_IMAGE="mcr.microsoft.com/azure-cli:2.77.0"
                          BUILD_ROOT="$(mktemp -d /tmp/change-audit-iac-plan.XXXXXX)"
                          SOURCE_DIR="$BUILD_ROOT/source"
                          ASKPASS="$BUILD_ROOT/git-askpass.sh"
                          CONTAINER_ID=""

                          cleanup() {
                            if [ -n "$CONTAINER_ID" ]; then
                              docker rm --force "$CONTAINER_ID" >/dev/null 2>&1 || true
                            fi
                            rm -rf "$BUILD_ROOT"
                          }
                          trap cleanup EXIT

                          command -v docker >/dev/null 2>&1 || { echo "docker is required on the delegate" >&2; exit 1; }
                          command -v git >/dev/null 2>&1 || { echo "git is required on the delegate" >&2; exit 1; }

                          printf '%s\n' '#!/bin/sh' \
                            'case "$1" in' \
                            '  *Username*) printf "%s\\n" "x-access-token" ;;' \
                            '  *Password*) printf "%s\\n" "$GITHUB_TOKEN" ;;' \
                            'esac' > "$ASKPASS"
                          chmod 700 "$ASKPASS"

                          GIT_ASKPASS="$ASKPASS" GIT_ASKPASS_REQUIRE=force GIT_TERMINAL_PROMPT=0 \
                            git clone --depth 1 --branch "$SOURCE_BRANCH" "$REPOSITORY_URL" "$SOURCE_DIR"

                          docker pull "$AZURE_CLI_IMAGE"
                          CONTAINER_ID="$(docker create \
                            --env AZURE_CLIENT_ID \
                            --env AZURE_CLIENT_SECRET \
                            --env AZURE_TENANT_ID \
                            --env AZURE_SUBSCRIPTION_ID \
                            --env AZURE_LOCATION \
                            --workdir /workspace \
                            "$AZURE_CLI_IMAGE" \
                            sh /workspace/infra/harness-deploy.sh plan)"
                          docker cp "$SOURCE_DIR/." "$CONTAINER_ID:/workspace"
                          docker start --attach "$CONTAINER_ID"
                    environmentVariables:
                      - name: GITHUB_TOKEN
                        type: String
                        value: <+secrets.getValue("github_pat")>
                      - name: AZURE_CLIENT_ID
                        type: String
                        value: <+secrets.getValue("azure_client_id")>
                      - name: AZURE_CLIENT_SECRET
                        type: String
                        value: <+secrets.getValue("azure_client_secret")>
                      - name: AZURE_TENANT_ID
                        type: String
                        value: <+secrets.getValue("azure_tenant_id")>
                      - name: AZURE_SUBSCRIPTION_ID
                        type: String
                        value: <+secrets.getValue("azure_subscription_id")>
                      - name: AZURE_LOCATION
                        type: String
                        value: <+pipeline.variables.azure_location>
                    outputVariables: []
                  timeout: 20m
              - step:
                  type: HarnessApproval
                  name: Approve infrastructure deployment
                  identifier: Approve_infrastructure_deployment
                  spec:
                    approvalMessage: Review the Bicep what-if output and approve the Azure deployment.
                    includePipelineExecutionHistory: true
                    approvers:
                      userGroups:
                        - account._account_all_users
                      minimumCount: 1
                      disallowPipelineExecutor: false
                    approverInputs: []
                  timeout: 1d
              - step:
                  type: ShellScript
                  name: Deploy Bicep
                  identifier: Deploy_Bicep
                  spec:
                    shell: Bash
                    executionTarget: {}
                    delegateSelectors:
                      - local-delegate
                    source:
                      type: Inline
                      spec:
                        script: |-
                          set -eu

                          REPOSITORY_URL="https://github.com/dosullivan557/Devops-coursework.git"
                          SOURCE_BRANCH='<+pipeline.variables.source_branch>'
                          AZURE_CLI_IMAGE="mcr.microsoft.com/azure-cli:2.77.0"
                          BUILD_ROOT="$(mktemp -d /tmp/change-audit-iac-apply.XXXXXX)"
                          SOURCE_DIR="$BUILD_ROOT/source"
                          ASKPASS="$BUILD_ROOT/git-askpass.sh"
                          CONTAINER_ID=""

                          cleanup() {
                            if [ -n "$CONTAINER_ID" ]; then
                              docker rm --force "$CONTAINER_ID" >/dev/null 2>&1 || true
                            fi
                            rm -rf "$BUILD_ROOT"
                          }
                          trap cleanup EXIT

                          command -v docker >/dev/null 2>&1 || { echo "docker is required on the delegate" >&2; exit 1; }
                          command -v git >/dev/null 2>&1 || { echo "git is required on the delegate" >&2; exit 1; }

                          printf '%s\n' '#!/bin/sh' \
                            'case "$1" in' \
                            '  *Username*) printf "%s\\n" "x-access-token" ;;' \
                            '  *Password*) printf "%s\\n" "$GITHUB_TOKEN" ;;' \
                            'esac' > "$ASKPASS"
                          chmod 700 "$ASKPASS"

                          GIT_ASKPASS="$ASKPASS" GIT_ASKPASS_REQUIRE=force GIT_TERMINAL_PROMPT=0 \
                            git clone --depth 1 --branch "$SOURCE_BRANCH" "$REPOSITORY_URL" "$SOURCE_DIR"

                          docker pull "$AZURE_CLI_IMAGE"
                          CONTAINER_ID="$(docker create \
                            --env AZURE_CLIENT_ID \
                            --env AZURE_CLIENT_SECRET \
                            --env AZURE_TENANT_ID \
                            --env AZURE_SUBSCRIPTION_ID \
                            --env AZURE_LOCATION \
                            --workdir /workspace \
                            "$AZURE_CLI_IMAGE" \
                            sh /workspace/infra/harness-deploy.sh apply)"
                          docker cp "$SOURCE_DIR/." "$CONTAINER_ID:/workspace"
                          docker start --attach "$CONTAINER_ID"
                    environmentVariables:
                      - name: GITHUB_TOKEN
                        type: String
                        value: <+secrets.getValue("github_pat")>
                      - name: AZURE_CLIENT_ID
                        type: String
                        value: <+secrets.getValue("azure_client_id")>
                      - name: AZURE_CLIENT_SECRET
                        type: String
                        value: <+secrets.getValue("azure_client_secret")>
                      - name: AZURE_TENANT_ID
                        type: String
                        value: <+secrets.getValue("azure_tenant_id")>
                      - name: AZURE_SUBSCRIPTION_ID
                        type: String
                        value: <+secrets.getValue("azure_subscription_id")>
                      - name: AZURE_LOCATION
                        type: String
                        value: <+pipeline.variables.azure_location>
                    outputVariables: []
                  timeout: 30m
        tags: {}
